# Fashion-MNIST Neural Network Assignment

This project implements a feedforward neural network from scratch using NumPy for classification on the Fashion-MNIST dataset.

## Task 1
- Loaded Fashion-MNIST dataset
- Visualized one sample image from each class

## Technologies Used
- Python
- NumPy
- Pandas
- Matplotlib
- TensorFlow/Keras (dataset loading only)
